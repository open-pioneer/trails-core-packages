name: Build Samples

on:
  pull_request:
    types:
      - opened
      - synchronize

concurrency:
  group: build-samples-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    uses: open-pioneer/trails-repo-automation/.github/workflows/shared-build-samples.yml@dev
