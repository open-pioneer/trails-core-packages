---
"@open-pioneer/runtime": minor
---

Deprecate the following methods on the `ApplicationContext`: `getLocale`, `setLocale`, `getSupportedLocales()`.
Use the alternatives provided by the new `LocaleService` instead.
